using UIKit;

namespace PlannerBusConductoresN_MAUI;

public class Program {
    // This is the main entry point of the application. If you want to use a different Application Delegate class from "AppDelegate" you can specify it here.
    static void Main(string[] args) {
        UIApplication.Main(args, null, typeof(AppDelegate));
    }
}